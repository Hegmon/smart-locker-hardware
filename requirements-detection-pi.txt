# Optional Raspberry Pi person-detection dependencies.
# Install numpy/OpenCV/GPIO on the Pi with apt first:
#   sudo apt install -y python3-numpy python3-opencv python3-rpi.gpio
# The QR scanner uses OpenCV's built-in QRCodeDetector, pyzbar/libzbar fallback,
# requests, and RPi.GPIO.
#   sudo apt install -y libzbar0
#
# Keep inference runtimes out of requirements.txt because tflite-runtime does
# not publish wheels for every desktop/Python combination.

# Preferred runtime for Python 3.11 and older.
tflite-runtime>=2.14,<3.0; (platform_machine == "aarch64" or platform_machine == "armv7l") and python_version < "3.12"

# LiteRT package for 64-bit Python 3.12+ where tflite-runtime wheels are unavailable.
ai-edge-litert>=1.3,<3.0; platform_machine == "aarch64" and python_version >= "3.12"

pyzbar>=0.1.9,<0.2.0
